/* function sum () {
    console.log(2+2)
} */

function sum(param1, param2, param3) {
  // parameters
  const sum = param1 + param2 + param3;
  return sum;
}

const getSum = sum(4, 4, 10);
console.log(getSum);
